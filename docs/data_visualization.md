# 데이터 시각화 디자인 시스템

> **범위**: 시각화 영역의 디자인 토큰, 컴포넌트 스펙, 렌더링 규칙, 데이터 집계·계산·예외 처리 규칙을 함께 다룬다.
> **연계**: 공통 디자인 토큰(폰트, 중립 색상, 반경, 그림자)은 `design_system.md`를 참조한다. 이 문서에는 시각화 영역 전용 적용 기준만 기술한다.
> **AI 작업 규칙**: 이 문서에 정의된 토큰과 컴포넌트만 사용한다. `design_system.md`와 이 문서에 없는 색상·크기·간격을 임의로 추가하지 않는다. 새 토큰이 필요하면 반드시 사용자에게 확인을 요청한다.

---

## 0. 기본 개념

### 분석 대상 문항
- 결과를 직접 시각화해서 보여주는 중심 문항이다.
- 결과 카드 제목은 기본적으로 코드북의 `question_label`을 사용한다.
- 질문 원문이 있으면 `Q. {question_full}` 형식으로 함께 표시한다.

### 분석 기준 문항
- 분석 대상 문항의 결과를 그룹별로 나누어 비교할 때 사용하는 문항이다.
- 현재 대시보드는 단일응답 계열 그룹 문항과 시스템 컬럼 `survey_year`를 기준 문항 후보로 사용한다.
- 기준 문항을 선택하지 않으면 전체 결과만 보여준다.

### 지원 문항 유형
- 객관식 단일
- 객관식 중복
- 객관식 순위
- 객관식 척도
- 주관식 숫자
- 주관식 시간
- 주관식 비율 배분
- 주관식 문자

### 비지원 문항 유형
- 위 지원 목록에 포함되지 않는 유형
- 비지원 문항은 결과 영역에 `아직 지원하지 않는 문항 형식` 안내를 표시한다.

---

## 1. 디자인 토큰 — 시각화 영역

### 1-1. 타이포그래피

| 요소 | 토큰 | Size | Weight | Color |
|------|------|-----:|--------|-------|
| 결과 카드 제목 | `Heading-4` | 18px | Bold (700) | `neutral-900` |
| 질문 원문 | `Body-2` | 16px | Medium (500) | `neutral-700` |
| 컨트롤바 레이블 | `Label-1-strong` | 14px | Semibold (600) | `neutral-700` |
| 축 레이블 (보기명, 그룹명 등) | `Label-1` | 14px | Regular (400) | `neutral-900` |
| 수치 데이터 레이블 | `Label-1-strong` | 14px | Semibold (600) | `neutral-700` |
| 차트 축 눈금 | `Caption-1` | 12px | Regular (400) | `neutral-600` |
| 범례 항목 | `Label-1` | 14px | Regular (400) | `neutral-800` |
| 범례 항목 추가 텍스트 | `Label-1` | 14px | Regular (400) | `neutral-600` |
| 데이터 테이블 헤더 | `Label-2-strong` | 13px | Semibold (600) | `neutral-700` |
| 데이터 테이블 셀 | `Label-2` | 13px | Regular (400) | `neutral-700` |
| 데이터 테이블 푸터 (합계행 등) | `Label-2-strong` | 13px | Semibold (600) | `neutral-700` |
| 데이터 테이블 토글 | `Label-1-strong` | 14px | Semibold (600) | `neutral-700` |
| 보충 설명 문구 | `Caption-1` | 12px | Regular (400) | `neutral-600` |
| 비지원 안내 문구 | `Caption-1` | 12px | Regular (400) | `neutral-600` |
| 툴팁 텍스트 | — | 11.5px | Regular (400) | `White` |

> 보충 설명 문구는 컨트롤바 하단 또는 데이터 테이블 하단에 위치하며, 위 요소와의 간격은 8px로 한다.

### 1-2. 컬러 팔레트 용도

#### 단일 색상 그래프
- 단일 색상을 사용하는 경우(가로/세로 막대 기본 차트, 가로/세로 롤리팝 기본 차트, 히스토그램 등) `neutral-700`을 기본으로 한다.

#### 여러 색상이 필요한 그래프
- **그룹 비교**: 그룹 순서대로 `color-1` → `color-2` → … → `color-20` 순환 할당. 최대 20개 그룹까지 색상 구분 가능.
- **객관식 단일 — 누적 막대 / 원형 그래프**: 보기 순서대로 `color-1` → `color-2` → … 순환 할당해 보기별 색상을 구분.

#### 순위 계열
- 누적 막대 차트에서 1/2/3순위 구간을 색상으로 구분할 때 사용한다. neutral 계열만 사용하되 명도로 순위를 구분한다.

| 순위 | 토큰 |
|------|------|
| 1순위 | `neutral-700` |
| 2순위 | `neutral-400` |
| 3순위 | `neutral-300` |
| 4순위 | `neutral-200` |
| 4순위 | `neutral-100` |

#### 척도 계열
- 1점(최저)은 `color-2`(붉은 계열), 중간점은 `neutral-300`, 최고점은 `color-1`(푸른 계열)을 기준으로 하고, 그 사이는 자연스러운 그라데이션 값으로 채운다.
- 5점 척도와 7점 척도는 아래 가이드를 따르고, 그 외 척도는 같은 방식으로 적당히 지정한다.

**5점 척도**

| 점수 | 토큰 |
|------|------|
| 1 | `color-2` |
| 2 | `low-3` |
| 3 | `neutral-300` |
| 4 | `high-3` |
| 5 | `color-1` |

**7점 척도**

| 점수 | 토큰 |
|------|------|
| 1 | `color-2` |
| 2 | `low-4` |
| 3 | `low-2` |
| 4 | `neutral-300` |
| 5 | `high-2` |
| 6 | `high-4` |
| 7 | `color-1` |

**그 외 척도**: 위 기준점(`color-2` → `neutral-300` → `color-1`)을 유지하면서 단계 수에 맞게 중간 토큰을 적절히 배정한다.

#### 박스플롯 계열
- 박스 내부 구간(Q1–Q2, Q2–Q3)은 두 구간을 같은 단일 색상으로 채운다.
- 단일 보기(응답자 전체)는 `neutral-400`을 사용한다.
- 그룹 비교는 그룹 비교 팔레트(`color-1` → `color-2` → … 순환)를 그대로 따른다.

#### 척도 평균 그래프 계열
- 분포 막대 배경의 각 구간 색상은 척도 분포 그래프와 동일한 색상을 사용하되, opacity 0.3을 적용한다.


---

## 2. 컴포넌트 스펙

### 2-1. 결과 카드

#### 결과 카드 구조

```
card-data
├── Data_Header (헤더)             ← flex column
│   ├── Header_Top                 ← flex row, justify-between
│   │   ├── 제목/질문 블록         ← flex column, gap 12px
│   │   │   ├── 카드 제목
│   │   │   └── 질문 원문
│   │   └── 액션 버튼
│   └── Control (컨트롤바)         ← 차트 유형에 따라 내용 다름
└── Data_Visualization (시각화 영역)
    ├── Chart (차트 행)
    │   ├── 차트 영역
    │   └── 범례 패널
    └── Table (테이블 섹션)
        ├── 토글 버튼
        └── 테이블 본문
```

#### 결과 카드 컨테이너

| 항목 | 값 |
|------|----|
| border-radius | `--radius-16` = 16px |
| border | 1px solid `neutral-300` |
| 배경 | `white` |
| 패딩 | 28px(상) / 28px(좌·우) / 40px(하) |
| 문항 구분선 위·아래 여백 | 각 48px (마지막 섹션 제외) |

#### 헤더 영역

헤더 상단은 제목/질문 블록과 액션 버튼을 좌우로 배치하고, 그 아래에 컨트롤바를 배치한다.
컨트롤바는 헤더 영역 내부(`.result-header`)에 포함된다. 
`1순위만 보기` 버튼은 객관식 순위형 문항에서만 제목 옆에 표시한다.

| 항목 | 값 |
|------|----|
| flex-direction | column |
| 제목 ↔ 질문 원문 간격 | 12px |
| 카드 제목 ↔ `1순위만 보기` 버튼 간격 | 12px (객관식 순위형 문항에서만 적용) |
| 액션 버튼 간 간격 | 8px |
| 액션 버튼 스타일 | `Button-2 Default` (→ `design_system.md` 참조) |
| 제목/질문 블록 ↔ 컨트롤바 간격 | 32px |

#### 시각화 영역

| 항목 | 값 |
|------|----|
| 차트 행 상단 여백 (↔ 컨트롤바) | 48px |
| 좌·우 내부 패딩 | 16px |
| 차트 영역 ↔ 범례 패널 gap | 52px |
| 범례 패널 너비 | 유동 180px ~ 280px (`calc(clamp(240px, 22vw, 340px) − 60px)`). 범례 유무와 관계없이 항상 공간 차지 |
| 차트 영역 너비 | grid 컬럼 `minmax(0, 1fr)` (남은 공간을 모두 차지) |
| 차트 행 하단 여백 (↔ 테이블 섹션) | 48px |

### 2-2. 컨트롤바

카드 헤더와 시각화 영역 사이에 위치하는 제어 영역이다.
컨트롤 조합은 차트 유형에 따라 달라진다. 아래 4가지 컨트롤 요소를 필요에 따라 조합해 사용한다.

**공통**

| 항목 | 기준 |
|------|------|
| 레이아웃 | flex row, items-center |
| 컨트롤 간 gap | 32px |
| 컨트롤 줄바꿈 row-gap | 8px |
| 너비 | 카드 내부 전체 너비 (100%) |
| 높이 | hug (내부 요소 높이에 맞춤) |

**① control_dropdown — 레이블 + 드롭다운**

레이블 텍스트와 드롭다운 트리거가 한 쌍을 이루는 컨트롤이다. 그래프 유형 선택 등에 사용한다.

> 드롭다운 컴포넌트 스펙은 `design_system.md`의 **Dropdown** 섹션을 참조한다.

| 항목 | 기준 |
|------|------|
| 레이아웃 | flex row, gap: 16px, items-center |
| 레이블 | `Label-1-strong` `neutral-700` |
| 트리거 최소 너비 | 180px |

**② control-checkbox — 체크박스 + 레이블**

단일 옵션을 on/off 하는 컨트롤이다. 정렬 옵션 등에 사용한다.

> 체크박스 컴포넌트 스펙은 `design_system.md`의 **Checkbox** 섹션을 참조한다.

| 항목 | 기준 |
|------|------|
| 레이아웃 | flex row, gap: 8px, items-center |
| 레이블 | `Label-1-strong` `neutral-700` |

**③ control-toggle — 세그먼트 토글 (2개 옵션)**

2가지 보기 모드를 전환하는 segmented control이다. 분포/평균 보기 전환 등에 사용한다.

| 항목 | 기준 |
|------|------|
| 레이아웃 | flex row, gap: 8px, items-center |
| 외곽 border | 1px solid `neutral-300` |
| 외곽 border-radius | `--radius-12` = 12px |
| 외곽 padding | 4px |
| **선택 버튼** 배경 | `neutral-900` |
| 선택 버튼 border-radius | `--radius-8` = 8px |
| 선택 버튼 패딩 | 상·하 4px / 좌·우 12px |
| 선택 버튼 텍스트 | `Label-1-strong` `white` |
| **미선택 버튼** 배경 | 없음 |
| 미선택 버튼 border-radius | `--radius-8` = 8px |
| 미선택 버튼 패딩 | 상·하 4px / 좌·우 12px |
| 미선택 버튼 텍스트 | `Label-1-strong` `neutral-700` |
| **비활성 버튼** 배경 | 없음 (선택 불가 상태) |
| 비활성 버튼 border-radius | `--radius-8` = 8px |
| 비활성 버튼 패딩 | 상·하 4px / 좌·우 12px |
| 비활성 버튼 텍스트 | `Label-1` `neutral-300` |

└ **전체 비활성 — 토글 전체가 비활성화된 상태** (③ 변형)

| 항목 | 기준 |
|------|------|
| 외곽 border | 1px solid `neutral-300` |
| 외곽 배경 | `neutral-100` |
| 버튼 배경 | 없음 |
| 버튼 텍스트 | `neutral-300` |
| pointer-events | none |

**④ label & input — 레이블 + 입력창**

레이블과 입력창이 한 쌍을 이루는 컨트롤이다. 숫자 입력은 구간 시작값·간격 조정 등에, 문자 입력은 주관식 문자 키워드 검색 등에 사용한다.

| 항목 | 기준 |
|------|------|
| 레이아웃 | flex row, gap: 8px, items-center |
| 레이블 | `Label-1-strong` `neutral-700` |
| 레이블 (비활성) | `neutral-300` |
| 숫자 입력창 너비 | 72px |
| 숫자 입력창 패딩 | 상·하 8px / 좌 10px / 우 6px |
| 문자 입력창 최소 너비 | 220px |
| 문자 입력창 패딩 | 상·하 8px / 좌 16px / 우 10px |
| 입력창 border | 1px solid `neutral-300` |
| 입력창 border-radius | `--radius-12` = 12px |
| 입력창 배경 (활성) | `white` |
| 숫자 입력창 배경 (비활성) | `neutral-100` |
| 숫자 입력창 텍스트 | `Button-2` `neutral-900` |
| 문자 입력창 텍스트 | `Label-2` `neutral-900` |
| 문자 입력창 placeholder | `Label-2` `neutral-400` |
| 입력창 텍스트 (비활성) | `neutral-300` |
| 입력창 텍스트 정렬 | 왼쪽 정렬 |
| 숫자 입력창 스피너 | 브라우저 기본 number input 스피너 (커스텀 없음) |
| 문자 입력창 검색 아이콘 | 20×20px, 입력 영역 오른쪽에 배치 |


### 2-3. 차트

#### 공통

> **방향 정의**: 막대가 가로로 뻗는 차트를 **가로 막대**, 막대가 세로로 뻗는 차트를 **세로 막대**라 한다. 가로 막대에서 보기명은 왼쪽, 비율은 오른쪽에에 표시된된다. 세로 막대에서 보기명은 하단, 비율은 위에 표시된다.

- 막대 border-radius: `--radius-4` (4px). 누적 막대는 첫 번째·마지막 구간에만 양끝 적용.
- 수치 레이블: `Label-1-strong` (→ 1-1 타이포그래피 참조). 차트 요소가 너무 좁을 때는 표시하지 않음.
- 보기명: `Label-1` (→ 1-1 타이포그래피 참조), `neutral-900`
- 막대 그래프 (1~5번, 11번 히스토그램) 공통 축 스타일: 축 색상 `neutral-300` / 눈금 색상 `neutral-200`
- 막대 hover: opacity 0.85
- 막대 전환 애니메이션: `width 0.2s ease` (가로 막대) / `height 0.2s ease` (세로 막대)

##### 1. 가로 막대

보기(항목)별 **선택 비율·빈도**를 막대 길이로 나란히 비교할 때 쓴다.

**주요 요소**

| 항목 | 값 |
|------|----|
| 막대 트랙 너비 | 차트 영역 100% 기준 (막대 길이는 응답 비율에 비례) |
| 막대 높이 | 32px |
| 행 간격 | 8px |
| 보기명 위치 | 좌측 고정 열, text-right |
| 보기명 열 너비 | 차트 너비의 20%, 최소 120px, 최대 220px |
| 보기명 ↔ 막대 gap | 16px |

**레이블**

| 항목 | 값 |
|------|----|
| 수치 레이블 위치 | 막대 바깥 오른쪽, 간격 8px |
| 수치 레이블 예외 | 막대가 90%에 육박해 우측 공간이 부족한 경우 막대 안쪽 우측에 표시, 막대 가장자리와 간격 8px |
| 수치 레이블 색상 | 막대 바깥: `neutral-700` / 막대 안쪽: `white` |

**축**

| 항목 | 값 |
|------|----|
| x(값)축 눈금 | 20% 간격으로 눈금 레이블 표시 (20%, 40%, 60%, 80%, 100%), `Caption-1` `neutral-600` (→ 1-1 **차트 축 눈금**) |
| 수직 그리드 라인 | x(값)축 눈금 위치마다 차트 영역 전체 높이에 걸쳐 표시, 색상 `neutral-200`. 가장 아래에 있는 막대보다 **8px** 더 내려온다. |

##### 2. 가로 막대 누적형

보기마다 응답을 **한 막대 안에서 다시 쪼개** 비중을 보고 싶을 때 쓴다. (예: 순위형 문항에서 보기별 **1·2·3순위** 비율)

**주요 요소**

| 항목 | 값 |
|------|----|
| 막대 트랙 너비 | 차트 영역 100% 기준 (막대 길이는 응답 비율에 비례) |
| 막대 높이 | 32px |
| 행 간격 | 8px |
| 보기명 위치 | 좌측 고정 열, text-right |
| 보기명 열 너비 | 차트 너비의 20%, 최소 120px, 최대 220px |
| 보기명 ↔ 막대 gap | 16px |
| 구간 색상 (순위형) | 1순위 `neutral-700` → 2순위 `neutral-400` → 3순위 `neutral-300` → 4순위 `neutral-200` → 5순위 `neutral-100` (좌→우) |

**레이블**

| 항목 | 값 |
|------|----|
| 구간 내 수치 레이블 | `Label-2`, 구간 내 우측 정렬 (px 8px), 구간이 좁으면 미표시 |
| 구간 내 수치 색상 | `neutral-700` 구간: `white` / `neutral-400·300·200·100` 구간: `neutral-700` |
| 전체 합계 레이블 | 차트 우측 영역에 표현, 너비 44px, 막대 트랙과의 간격 8px, 텍스트 영역 내 중앙 정렬, `Label-1-strong` `neutral-700` |

**축**

→ 1. 가로 막대 축 참조. 단, 수직 그리드 라인은 우측 합계 레이블 열을 제외한 막대 트랙 영역까지만 표시한다.

##### 3. 가로 막대 100% 기준 누적형

한 막대(또는 한 조각) 안에서 **분할 단위별 비율만** 보여 주고 싶을 때 쓴다. 분할 단위(보기·점수 구간·배분 항목 등)별 **응답 비율이 합쳐져 100%**를 이룰 때 쓸 수 있는 차트이다.

**주요 요소**

| 항목 | 값 |
|------|----|
| 막대 높이 | 40px |
| 각 구간 너비 | 분할 단위별 비율에 비례 |

**레이블**

| 항목 | 값 |
|------|----|
| 수치 레이블 (구간 내부) 위치 | 구간 내부 수직·수평 중앙. 세그먼트가 지나치게 좁으면 미표시 |
| 하단 레이블 ↔ 막대 | 간격 8px (막대 하단선에서 하단 레이블 영역 상단까지) |
| 하단 레이블이 두 줄인 경우 | 위→아래 순서, 줄 간 간격 0. **텍스트 레이블**이면 `Label-1`, **수치 레이블**이면 `Label-1-strong`. |

구간 색상, 범례, 구간·하단 레이블의 **문구·종류·상세 정렬·색·타이포(한 줄인 경우 포함)** 등은 문항·응답 형식에 따르므로 **`## 4. 문항 응답 형식별 시각화 규칙`**(4-1, 4-4, 4-6 등), 척도 구간 색은 1-2 **척도 계열**, 그 밖의 막대 공통 토큰은 위 **2-3. 차트 > 공통** 단락을 참조한다.

##### 4. 세로 막대

`##### 1. 가로 막대`와 같이 보기별 **비율·빈도**를 비교하되, **세로 막대**로 표현할 때 쓴다.

**주요 요소**

| 항목 | 값 |
|------|----|
| 값축·막대 트랙 높이 | 280px 고정 (y(값)축 0~100% 눈금·막대가 그려지는 영역. 보기명·상단 수치 레이블 미포함) |
| 플롯 상단 여백 | 24px (`--viz-vertical-top-space`). 막대가 스케일 상한에 가까울 때 **위쪽 수치 레이블**이 플롯 밖으로 잘리지 않도록 트랙 위에 확보 |
| 막대 너비 | 유동, 최대 120px |
| 막대 간 gap | 4px (막대가 최대 너비 초과 시 간격이 넓어짐) |
| 막대 끝(위) border-radius | 좌상·우상만 `--radius-4` |

**레이블**

| 항목 | 값 |
|------|----|
| 보기명 위치 | 막대 하단, 막대 너비에 맞춰 표기 |
| 보기명 ↔ 가로축 간격 | 8px |
| 보기명 좌우 패딩 | 8px |
| 수치 레이블 위치 | 막대 끝(위) 바깥, 간격 2px |
| 수치 레이블 색상 | `neutral-700` |

**축**

| 항목 | 값 |
|------|----|
| y(값)축 눈금 | 20%마다(가장 높은 막대 값을 감싸도록 상한을 올림). 우측에 `%` 단위 눈금 레이블, `Caption-1` `neutral-600` (→ 1-1 **차트 축 눈금**) |
| 수평 그리드 라인 | y(값)축 눈금 높이마다 차트 플롯 영역 전체 너비에 걸쳐 표시, 색상 `neutral-200` |
| 가로축 기준선 | 막대·눈금 영역 하단 경계(카테고리 축), 색상 `neutral-300` |

##### 5. 세로 막대 누적형

보기마다 응답을 **세로 막대 한 개 안에서 다시 쪼개** 비중을 보고 싶을 때 쓴다. (예: 순위형 문항에서 보기별 **1·2·3순위** 비율)

`##### 4. 세로 막대`와 **주요 요소**의 값축·트랙 높이·상단 여백·막대 너비·막대 간 gap, **레이블**의 보기명 관련 항목, **축** 전체를 공유한다. 아래는 누적형 전용이다.

**주요 요소**

| 항목 | 값 |
|------|----|
| 최상단 구간 border-radius | 좌상·우상만 `--radius-4` |
| 구간 색상 (순위형) | 1순위 `neutral-700` → 2순위 `neutral-400` → 3순위 `neutral-300` → 4순위 `neutral-200` → 5순위 `neutral-100` (쌓인 순서; `##### 2. 가로 막대 누적형`과 동일 토큰) |

**레이블**

| 항목 | 값 |
|------|----|
| 구간 내 수치 레이블 | `Label-2`, 막대 안쪽에서 2px 간격으로 위로 붙임, 구간이 좁으면 미표시 |
| 구간 내 수치 색상 | `neutral-700` 구간: `white` / `neutral-400·300·200·100` 구간: `neutral-700` |
| 전체 합계 레이블 | 막대 끝(위) 바깥, 막대 상단과 간격 2px (`##### 4` 단일 세로 막대 수치 레이블과 동일), `Label-1-strong` `neutral-700` |

##### 6. 롤리팝 (가로)

평균·가중 평균 등 **점수형 요약값**을 보기별로 비교할 때 쓴다. (예: 순위형 문항에서 보기별 점수 비교 — 가중 평균이 대표적이나 유형·정의에 따라 다를 수 있음)

**주요 요소**

| 항목 | 값 |
|------|----|
| 트랙 높이 | 32px, border-radius `--radius-4` (4px) |
| 줄기 높이 | 8px (`--lollipop-stem-width`; 헤드 고정 단부 기준) |
| 헤드 (원) 지름 | 16px |
| 헤드·줄기 색상 | `neutral-700` (원과 줄기 동일) |
| 트랙 간 간격 | 8px (보기마다 한 줄씩 두는 롤리팝 **트랙**과 다음 트랙 사이) |
| 보기명 위치 | 좌측 고정 열, text-right (→ `##### 1. 가로 막대`와 동일) |
| 보기명 열 너비 | 차트 너비의 20%, 최소 120px, 최대 220px |
| 보기명 ↔ 줄기 gap | 16px |

**레이블**

| 항목 | 값 |
|------|----|
| 수치 레이블 위치 | 헤드 바깥 오른쪽, 간격 8px |
| 수치 레이블 색상 | `neutral-700`, `Label-1-strong` |
| 수치 레이블 형식 | 소수 둘째 자리까지 표시 (예: `3.24`) |
| 순위 레이블 위치 | 차트 영역 우측의 별도 공간에 표시, 너비 44px, 수치 레이블과의 간격 8px, 텍스트 영역 내 중앙 정렬, `Label-1-strong` `neutral-700` |

**축**

| 항목 | 값 |
|------|----|
| 가로축 눈금선 | 세로 방향 선, 색상 `neutral-200`, 차트 전체 너비에 걸쳐 표시 |
| 가로축 눈금 간격 | 1점 간격 |
| 가로축 범위 | 0(좌끝) ~ 문항에 정의된 **최고 점수(만점)**에 맞춘 우끝. (표시하는 요약 점수의 상한; 순위형 등 구체 규칙은 `4-3. 객관식 순위` 참조) |
| 가로축 눈금 레이블 | 눈금선 하단, `Caption-1` `neutral-600` (→ 1-1 **차트 축 눈금**) |

##### 7. 롤리팝 (세로)

평균·가중 평균 등 **점수형 요약값**을 보기별로 세로 축 위에서 비교할 때 쓴다. (`##### 6. 롤리팝 (가로)`와 같은 해석을 세로 레이아웃으로 옮긴 형태.)

**값축·플롯 트랙 높이·상단 여백**은 `##### 4. 세로 막대` **주요 요소**와 동일하다. (막대 대신 세로로 이어진 줄기·헤드가 올라간다고 보면 된다.)

**주요 요소**

| 항목 | 값 |
|------|----|
| 헤드 (원) 지름 | 16px |
| 헤드·줄기 색상 | `neutral-700` (원과 줄기 동일) |
| 줄기 너비 | 8px (`--lollipop-stem-width`; 헤드 고정 단부 기준) |
| 보기(열) 간 간격 | 4px (`--viz-vertical-col-gap`; 트랙 행·보기명 행에서 인접 열 사이. 세로 막대 **막대 간 gap**과 동일 토큰) |

**레이블**

| 항목 | 값 |
|------|----|
| 보기명 | `##### 4` **레이블**의 보기명 관련 항목과 동일 |
| 수치 레이블 위치 | 헤드 상단 2px 위 |
| 수치 레이블 색상 | `neutral-700`, `Label-1-strong` |
| 수치 레이블 형식 | `##### 6` **레이블**과 동일 (소수 둘째 자리까지) |
| 순위 레이블 위치 | 수치 레이블 상단, 간격 2px |

**축**

→ `##### 4. 세로 막대` **축**과 같은 배치·선 스타일이다. 값축만 **0 ~ 만점 점수**이며, 눈금은 **1점 간격·정수 레이블**(`%` 없음)이다. 범위·만점은 `##### 6` **축**과 같은 원칙이다.

##### 8. 원형 (파이) 그래프

보기(또는 구간)별 비율의 **합이 100%**일 때, 한 원에서 **점유 비율**을 나눠 보여 줄 때 쓴다. (`##### 3. 가로 막대 100% 기준 누적형`과 같이 **전체=100%**라는 전제를 공유한다.)

**주요 요소**

| 항목 | 값 |
|------|----|
| 크기 | 280px × 280px 고정 |
| 가로 정렬 | 차트 영역 가로 중앙 |
| 구간 색상 | 보기 순서대로 `color-1` → `color-2` → … 순환 할당 |

**레이블**

| 항목 | 값 |
|------|----|
| 수치 레이블 위치 | 구간 내부 중앙 기준. 구간이 좁아 내부 표시가 어려우면 호 중앙 바깥쪽에 적당한 간격을 두고 표시 |
| 수치 레이블 색상 | 내부: `white` / 외부: `neutral-700` |
| 수치 레이블 타이포 | `Label-1-strong` |

##### 9. 척도 평균 그래프

척도 문항에서 **`##### 3. 가로 막대 100% 기준 누적형`**과 같은 방식으로 만든 **100% 기준 가로 누적 막대**를 배경으로 깔아 두고(점수 구간별 비율 너비), 그 위에 **평균 위치**를 한눈에 보여 줄 때 쓴다.

평균값은 **원형** 마커로 표시한다. 배경 구간은 척도 분포 막대와 **같은 점수별 색**에 **opacity 0.3**을 적용한다(→ 1-2 **척도 평균 그래프 계열**).

**주요 요소 — 분포 막대**

| 항목 | 값 |
|------|----|
| 분포 막대 높이 | 40px |
| 분포 막대 구조 | `##### 3. 가로 막대 100% 기준 누적형`과 동일한 방식으로 점수 구간별 비율 너비를 산출하고, 그 너비를 배경 막대에 그대로 사용 |
| 분포 막대 좌끝·우끝 | `--radius-4` (좌끝: 좌상·좌하, 우끝: 우상·우하) |
| 구간 색상 | 척도 분포와 동일한 점수별 색 + **opacity 0.3** (→ 1-2 **척도 평균 그래프 계열**) |

**주요 요소 — 평균 마커**

| 항목 | 값 |
|------|----|
| 지름 | 40px |
| 배경 | `neutral-900`, **원** (`width`·`height` 동일, `border-radius: 50%`) |
| 위치 | 분포 막대와 높이가 동일(40px)하며, 막대 위에 정확히 겹쳐 배치 |

**레이블**

| 항목 | 값 |
|------|----|
| 척도점 레이블 | 막대 하단, 간격 8px, `Label-1` `neutral-600` |
| 척도 끝점 레이블 | 척도점 레이블 하단, 간격 0. 좌끝(1점)에는 최저점 보기명 좌측 정렬, 우끝(최고점)에는 최고점 보기명 우측 정렬. `Label-1` `neutral-700`. 예: 좌 `전혀 그렇지 않다` / 우 `매우 그렇다`. 보기명 텍스트가 없는 문항은 표시하지 않는다. |
| 평균 마커 값 | `Label-1-strong` `white` |
| "평균" 텍스트 | 마커 바로 위, 간격 0, `Label-2-strong` `neutral-700` |

**축**

| 항목 | 값 |
|------|----|
| 척도점 눈금 | 점수마다 수직 tick **20px** |
| 선 색상 | `--viz-axis-color` |
| 배치 | **막대 세로 중앙**에 겹쳐 표시 |

##### 10. 박스플롯

연속형 값(숫자 응답·파생 척도 등)의 **분포·사분위·극단값**을 한 박스에 요약해 보여 줄 때 쓴다.

**주요 요소 — 박스·수염**

| 항목 | 값 |
|------|----|
| 박스 높이 | 40px |
| 박스 구조 | flex row: [whisker 좌] [Q1→Q2] [Q2→Q3] [whisker 우] |
| whisker 구간 | 투명 배경, 선 색상 `neutral-600`, 최솟값 ~ Q1 / Q3 ~ 최댓값 |
| Q1→Q2 / Q2→Q3 구간 색상 | 박스플롯 계열 참조 (→ 1-2 컬러 팔레트) |
| Q1·Q2·Q3 tick | 각 위치에 세로 40px 길이의 tick 선을 박스 위에 표시한다. 색상은 `neutral-600` |

**주요 요소 — 평균 마커**

| 항목 | 값 |
|------|----|
| 평균 마커 | 9번 척도 평균 그래프와 동일 (`neutral-900` 원형 마커, 흰 텍스트, "평균" 위에 표시) |
| 평균 레이블 소수점 | 소수점 첫째 자리. Derived Scale 박스플롯은 소수점 둘째 자리 |

**레이블**

| 항목 | 값 |
|------|----|
| Q1·Q2·Q3 레이블 | 막대 하단, 간격 8px, `Label-1-strong` `neutral-900` |
| Q1·Q2·Q3 값 | 레이블 바로 아래, 간격 0, `Label-1` `neutral-900` |
| Q1·Q2·Q3 클릭 | 한 블록을 누르면 나머지는 **연하게**(opacity **0.15**), 누른 블록만 **강조**된다. 같은 블록을 다시 누르거나 차트 바깥을 누르면 모두 원래 상태로 돌아온다. |
| 단위 문구 | **값 단위**가 정의된 경우에만(예: 코드북 `numberUnit`), 차트 블록 **우하단**에 `단위 : …`를 붙인다. 차트 본문과 **8px** 간격, 우측 정렬, `Label-2` regular `neutral-600`. |

**축**

| 항목 | 값 |
|------|----|
| 가로축 | **Q 레이블까지 포함한** 박스플롯 차트 블록 **하단에서 32px** 아래에 가로 축 선을 그린다. 선·눈금 색 `neutral-300`, 눈금 **10px**(축 아래로만). 눈금 레이블은 축 영역 **상단에서 12px** 아래, `caption-2` `neutral-600`. 범위는 값 분포에 맞게 둔다. |

**평균 강조 모드**

여러 문항 보기에서 평균 모드로 전환 시 박스 구간(`Q1→Q2`, `Q2→Q3`)의 opacity를 0.3으로 낮춰 평균 마커를 강조한다.

##### 11. 히스토그램

**연속형 값**(숫자 응답 등)을 **숫자 구간**으로 나눈 뒤, 구간별 **빈도(비율)**를 막대 높이로 보고 싶을 때 쓴다.

세로 플롯의 **값축·트랙 높이·상단 여백**, **축**(y(값)축 눈금, 수평 그리드 라인, 가로축 기준선), 막대 열 간격 등은 `##### 4. 세로 막대` **주요 요소**·**축**을 따른다. 아래는 히스토그램에만 해당하는 차이이다.

**주요 요소**

| 항목 | 값 |
|------|----|
| 구간 시작값·간격 | 사용자 조정 가능 |
| 평균 마커 | 세로 **2px** `neutral-900` 막대가 플롯 **전 높이**를 가로지른다(끝 `border-radius`로 둥글게). 막대 **오른쪽 8px**·**상단**에 `평균`과 그 **아래** 평균값을 둔다. `평균`은 `Label-2` regular `neutral-700`, 값은 같은 크기 **semibold** `neutral-700`, 숫자는 **소수 첫째 자리**까지(시간 등은 코드북 포맷).|

**레이블**

| 항목 | 값 |
|------|----|
| 가로축 레이블 | 보기명 없음. 막대 사이 경계값(구간 시작값)을 `Label-2` `neutral-600`으로 표기 |
| 단위 문구 | **값 단위**가 정의된 경우에만(예: 코드북 `numberUnit`), 차트 블록 **우하단**에 `단위 : …`를 붙인다. 차트 본문과 **8px** 간격, 우측 정렬, `Label-2` regular `neutral-600`. |

**축**

| 항목 | 값 |
|------|----|
| 세로축 단위 | 응답 비율 (%) |
| 그 외 | `##### 4` **축**과 동일 (도입 문단 참고) |

##### 12. 그룹별 비교

기준 문항으로 응답을 나눈 뒤, 그룹마다 값이 어떻게 다른지 한 화면에서 비교할 때 쓴다. 같은 차트 안에 **겹쳐** 보여 주는 방식과, **행을 세로로 쌓아** 나열하는 방식이 있다. 아래에 나온 차트만 그룹 비교를 지원한다.

**지원 차트**

| 차트 | 방식 |
|------|------|
| 가로 막대 | 겹침(도트) / 그룹 2개일 때 이중 막대 |
| 롤리팝(가로) | 겹침(도트) / 그룹 2개일 때 이중 롤리팝 |
| 100% 기준 누적 가로 막대 | 세로 나열(lane-group) |
| 척도 평균 그래프 | 세로 나열(lane-group) |
| 박스 플롯 | 세로 나열(lane-group) |

---

**(1) 가로 막대·롤리팝(가로) — 도트로 겹쳐 보기**

응답자 전체 기준 막대(또는 롤리팝)를 연한 회색(`neutral-200`)으로 깔아 두고, 그 위에 그룹별 값을 **지름 16px** 도트로 겹친다. 도트 색은 범례의 그룹 색과 같고, 테두리는 흰색이다. 도트를 누르면 그 그룹만 남기고 나머지는 흐리게 하며, 해당 그룹의 수치 레이블을 도트에 붙인다. 수치는 도트 **오른쪽**이 기본이나, 막대가 길어 오른쪽 여백이 부족하면 잘리지 않도록 도트 **왼쪽**에 둔다.

막대·축·수직 눈금선 등 겹침 모드의 골격은 일반 `##### 1. 가로 막대` 또는 `##### 6. 롤리팝 (가로)`와 같다.

---

**(2) 그룹이 둘일 때 — 이중 막대·이중 롤리팝**

범례에서 표시 중인 그룹이 **두 개만** 남으면, 범례 패널에 `두 그룹만 비교하기`가 켜진다. 이걸 쓰면 배경용 회색 차트는 숨기고, **보기(행)마다** 막대 또는 롤리팝 트랙을 **세로로 두 줄** 쌓아 두 그룹을 나란히 본다.

- 한 보기 안에서 두 막대(또는 두 롤리팝 트랙) 사이: **4px**
- 보기와 보기(행) 사이: **16px**
- 막대 쪽 축·눈금·수치 레이블 규칙은 `##### 1. 가로 막대`와 동일하다.
- 막대 채움색과 롤리팝 줄기·도트(헤드) 색은 모두 범례의 그룹 색과 같다.

---

**(3) 100% 누적·척도 평균·박스 — lane-group으로 세로 나열**

응답자 전체와 각 그룹을 **위에서 아래로** 다른 행에 그린다. 공통 레이아웃 클래스는 `.lane-group-chart`이며, 다음을 공유한다.

- **순서**: 맨 위는 항상 `응답자 전체` 한 행 → 가로 구분선(`lane-group-divider`, 좌측은 라벨 열 폭만큼 비움) → 그 아래로 그룹별 행.
- **행 사이**: 컨테이너 세로 `gap` **16px**. 구분선에는 위·아래 **8px** 마진을 더 둔다.
- **세로 치수**: **40px**. 누적 막대 트랙(`stack100-track`), 척도 분포/평균 전용 행, 박스 행, 평균 원 지름이 이 토큰에 맞춰진다.

**100% 기준 누적 가로 막대**  
막대 아래에 붙이던 보기명 레이블은 그룹 모드에서는 두지 않고, 보기 이름은 범례에서만 본다. 구간 색·비율 표기 등 나머지 골격은 `##### 3. 가로 막대 100% 기준 누적형`을 따른다.

**척도 평균 그래프**  
분포 막대 모드와 평균만 보는 모드 모두 위 lane-group 규칙을 쓴다. 그룹 나열에서는 평균 마커 위의 `평균` 텍스트와 원 안 숫자 레이블을 빼 간단히 한다.

**박스 플롯**  
박스 행 높이와 평균 원은 위와 같이 40px 토큰으로 맞춘다. 평균 쪽 레이블은 척도와 같이 줄이고, `Q1`·`Q2`·`Q3` 문구는 표시하지 않는다.

---

**그룹 비교를 쓰지 않는 차트**

세로 막대, 가로·세로 막대 누적형, 롤리팝(세로), 원형(파이), 히스토그램은 그룹별 보기를 제공하지 않는다.


### 2-4. 범례 패널

#### 1. 기본 기능

차트 우측 열에 배치되는 범례 패널이다.  

##### 범례 레이아웃

`.result-visual`은 두 열 그리드(`grid-template-columns: minmax(0, 1fr) var(--viz-legend-width)`)로 차트와 범례를 나란히 두며, 차트 열이 남은 가로 공간을 차지하고 범례 열 너비는 `--viz-legend-width` 토큰에 따른다(위 **`2-1. 결과 카드` > 시각화 영역** 표의 범례 패널 너비와 동일).

범례의 기본 구성은 `체크박스 + 색상 swatch + 항목명`이다.  
체크박스는 단순히 현재 색상과 항목을 보여주는 장식이 아니라, 해당 항목을 차트와 표에서 표시할지 숨길지를 사용자가 직접 제어하는 토글이다.  
경우에 따라 체크박스나 일괄 액션을 두지 않는 범례도 있다(아래 **범례의 종류** 및 **`## 4. 문항 응답 형식별 시각화 규칙`** 참고).

체크박스는 `design_system.md`의 Checkbox 컴포넌트 스펙을 따른다.

##### 범례의 유형

범례 **유형**마다 색을 대응시키는 대상·체크박스 유무·액션 버튼을 아래에 정리한다.
어떤 문항·차트에서 어떤 범례가 붙는지는 **`## 4. 문항 응답 형식별 시각화 규칙`**을 참고한다. 

| 범례 종류 | 색이 붙는 대상 | 체크박스 | 액션 버튼 |
|-----------|----------------|----------|-----------|
| **그룹 범례** | 비교 집단(그룹)마다 하나의 swatch | 있음 — 그룹 행마다, 차트·표에 그 그룹을 넣을지 고름 | `전체 선택`, `전체 해제`, `그룹 편집`, `두 그룹만 비교하기`※ |
| **순위 범례** | 1순위·2순위 등 순위마다 하나의 swatch | 있음 — 순위마다, 그 순위 응답을 차트·표에 넣을지 고름 | `전체 선택`, `전체 해제` |
| **보기 범례** | 응답 보기(선택지)마다 하나의 swatch | 없음 | 없음 |
| **점수 범례** | 척도 점수(및 코드북에 연결된 그 점수의 보기 문구)마다 하나의 swatch | 없음 | 없음 |

- ※ **`두 그룹만 비교하기`**: 일부 그룹 비교 차트에서만 표시되며, 범례에 남겨 둔(체크된) 그룹이 **정확히 둘**일 때만 활성화된다.

범례 패널은 공통적으로, 필요하면 서로 다른 종류의 범례를 둘 이상 한 패널에 둘 수 있으며, 그럴 경우 구획 사이에 구분선을 둔다.

**범례 기본 스펙**

| 항목 | 기준 |
|------|------|
| 너비 | `var(--viz-legend-width)` (수치·식은 `2-1` 시각화 영역 표와 동일) |
| 패딩 | 없음 |
| **줄 사이** 간격 | 세로로 쌓인 **범례 행**과 행 사이, `gap: 8px` |
| **한 줄 안** 간격 | **같은 행**에서 체크박스·swatch·텍스트 사이, `gap: 8px` |
| 체크박스·swatch 크기 | 14px × 14px |
| swatch border-radius | 4px |
| 범례 항목 텍스트 | `Label-1`, `neutral-800` |
| 범례 항목 추가 텍스트 | `Label-1`, `neutral-600` (예: 점수 옆 보기 문구) |
| 비활성 텍스트 | `neutral-300` |
| 비활성 swatch | opacity 0.28 |
| 항목 블록 ↔ 액션 첫 줄 | 세로 간격 **24px** |
| 액션 버튼끼리 | 가로 간격 **8px** (줄 바꿈 가능) |
| `두 그룹만 비교하기` ↔ 위 액션 줄 | 세로 간격 **8px** |

##### 범례 액션 버튼

**그룹 범례**·**순위 범례**에만 아래 버튼 묶음이 올 수 있다. **보기 범례**·**점수 범례**에는 액션 버튼을 두지 않는다(위 **범례의 종류** 표).

| 버튼 | 붙는 범례 유형 | 역할 |
|------|----------------|------|
| `전체 선택` | 그룹 범례, 순위 범례 | 그 범례 구획의 체크박스를 모두 켠다 |
| `전체 해제` | 그룹 범례, 순위 범례 | 모두 끈다 |
| `그룹 편집` | 그룹 범례만 | `범례 그룹 편집` 모달을 연다 |
| `두 그룹만 비교하기` | 일부 그룹 범례만 | 표시 그룹이 정확히 둘일 때만 활성화(위 **범례의 종류** ※). 켜면 두 그룹만 나란히 보는 차트 모드로 전환 |

- `design_system.md`: `전체 선택`·`전체 해제`는 **`Button-2 Default`**, `그룹 편집`은 **`Button-2 Gray`**, `두 그룹만 비교하기`는 **`Button-2 Black`**.

#### 2. 범례 그룹 편집 모달

**진입**

- 범례에서 `그룹 편집` 버튼을 누르면 `범례 그룹 편집` 모달이 열린다.
- 사용자는 여러 범례 항목을 하나의 그룹으로 묶어 차트와 데이터 테이블에서 비교 단위를 재구성할 수 있다.

**모달 구조**

모달은 헤더, 본문, 푸터로 구성한다.

**모달 헤더**

| 항목 | 기준 |
|------|------|
| 타이틀 | `범례 그룹 편집` |

**모달 본문**

| 영역 | 기준 |
|------|------|
| 범례 항목 리스트 | 현재 표시 가능한 모든 원본 범례 항목을 리스트로 노출 |
| 선택 방식 | 체크박스를 통한 다중 선택 |
| 그룹 리스트 | 생성된 그룹을 아코디언 형태로 표시 |

**모달 푸터**

| 항목 | 기준 |
|------|------|
| 위치 | 모달 하단 |
| 구성 | 좌측 `+` 아이콘이 포함된 `그룹 만들기` 버튼, 우측 `편집 완료` 버튼 |
| 버튼 스타일 | `그룹 만들기`는 `Button-1 Default`, `편집 완료`는 `Button-1 Black`, 비활성 시 `Button-1 Inactive` |
| 역할 | 편집 중 그룹 생성과 최종 적용 액션 제공 |
| 버튼 활성화 조건 | `그룹 만들기`는 선택 항목이 1개 이상일 때 활성화, `편집 완료`는 그룹 생성, 그룹명 수정, 그룹 해제 등 변경 사항이 있을 때 활성화 |

**범례 항목 리스트**
- 각 항목은 체크박스로 선택 가능하며, 선택된 항목만 새 그룹 후보가 된다.
- 이미 특정 그룹에 포함된 항목은 원본 리스트에서 선택할 수 없는 상태로 표시한다.
- 동일 항목은 둘 이상의 그룹에 포함될 수 없다.
- 선택 항목이 없으면 `그룹 만들기` 버튼은 비활성화되고, 하나 이상 선택되면 활성화된다.

**그룹 만들기**
- `그룹 만들기`를 클릭하면 현재 선택된 항목으로 새 그룹을 즉시 생성한다.
- 생성된 그룹은 그룹 리스트에 즉시 추가되며, 아코디언은 기본 펼침 상태로 노출한다.
- 생성 직후 그룹명 input은 자동으로 활성화되어야 하며, focus를 즉시 이동한다.
- 생성 직후 그룹명 input의 텍스트는 전체 선택 상태가 되어 사용자가 바로 새 이름을 입력할 수 있어야 한다.

**그룹 리스트 (아코디언)**
- 각 그룹은 아코디언 한 개 단위로 표현한다.
- 헤더에는 그룹명과 액션 아이콘을 함께 배치한다.
- 본문에는 해당 그룹에 포함된 원본 범례 항목 목록을 표시한다.

예시 구조:
```text
▽ 그룹명
항목1
항목2
```

- 그룹명은 inline input 방식으로 수정한다.
- 이름 확정 조건은 `Enter` 입력 또는 input 외부 클릭이다.
- 생성 직후 이름을 입력하지 않고 확정하면 기본 이름을 자동 부여한다. 기본 이름 규칙은 `그룹1`, `그룹2`, `그룹3` 순서를 따른다.
- 그룹명 중복은 허용한다.

**그룹 액션**

- 각 그룹 우측에는 `edit` 아이콘 버튼을 두고, 클릭 시 해당 그룹명 input을 다시 활성화한다.
- 각 그룹 우측에는 `link_off` 아이콘 버튼을 둔다.
- `link_off` 클릭 시 해당 그룹은 삭제되며, 그룹에 포함되어 있던 항목은 모두 원본 범례 상태로 복원된다.
- 편집 도중 그룹 내 항목 수가 0개가 되는 상태는 허용하지 않는다. 결과적으로 비어 있는 그룹이 발생하면 해당 그룹은 유지하지 않고 해제 처리한다.

**그룹 내 항목 제거**

- 그룹 본문에 표시된 각 멤버 항목 우측에 `remove` 아이콘 버튼을 두고, 클릭 시 해당 항목을 그룹에서 제거한다.
- 제거된 항목은 원본 범례 리스트로 복원되어 다시 선택 가능해진다.
- 항목 제거 결과 그룹이 비어지면 그룹 자체가 해제 처리된다.

**편집 완료**
- `편집 완료` 클릭 시 현재 그룹 편집 결과를 적용하고 모달을 닫는다.
- 적용 전후 변경 사항이 없으면 버튼은 비활성화 상태를 유지한다.

**데이터 반영**

- 그룹은 항상 현재 문항의 원본 범례 항목을 기준으로 생성한다.
- 생성된 그룹은 저장 후 새로운 범례 항목으로 적용된다.
- 그룹에 포함된 원본 범례 항목은 기본 범례 리스트에서 직접 표시하지 않는다.
- 그룹에 포함되지 않은 항목은 기존 범례 항목으로 그대로 유지한다.
- 최종 그룹 구성은 차트와 데이터 테이블 양쪽에 동일하게 반영한다.

**예외 처리**

- 선택 항목이 없으면 그룹 생성은 불가하다.
- 동일 항목을 둘 이상의 그룹에 중복 포함할 수 없다.
- 그룹명 중복은 허용한다.
- 빈 그룹은 허용하지 않으며, 항목이 모두 제거된 경우 그룹 해제로 처리한다.

### 2-5. 데이터 테이블

**셀 공통**

| 항목 | 기준 |
|------|------|
| 셀 높이 | 35px |
| 셀 패딩 | 8px 16px |
| 글꼴 | `Label-2` / `Label-2-strong` |
| line-height | 1.6 |
| letter-spacing | 0 |

**헤더 셀**

| 항목 | 기준 |
|------|------|
| 배경 | `neutral-50` |
| 글꼴 | `Label-2-strong` |
| 텍스트 색상 | `neutral-700` |
| 상단 보더 | 1px solid `neutral-300` |
| 하단 보더 | 1px solid `neutral-300` |
| 우측 보더 | 1px solid `neutral-300` (첫 번째 열 헤더에만 적용) |

**본문 셀**

| 항목 | 기준 |
|------|------|
| 배경 | `white` |
| 글꼴 | `Label-2` |
| 텍스트 색상 | `neutral-700` |
| 하단 보더 | 1px solid `neutral-300` |
| 우측 보더 | 1px solid `neutral-300` (첫 번째 열 셀에만 적용) |

**합계 행 (footer)**

| 항목 | 기준 |
|------|------|
| 배경 | `neutral-50` |
| 글꼴 | `Label-2-strong` |
| 텍스트 색상 | `neutral-700` |
| 상단 보더 | 1px solid `neutral-300` |
| 하단 보더 | 1px solid `neutral-300` |
| 우측 보더 | 1px solid `neutral-300` (첫 번째 열에만 적용) |

**컬럼 정렬**

| 컬럼 유형 | 정렬 |
|-----------|------|
| 응답 보기 (레이블 열) | 좌측 정렬 (`text-align: left`) |
| 응답 비율, 응답 수 등 수치 열 | 우측 정렬 (`text-align: right`) |

**강조 셀 (colored cell)**

순위(rank) 문항에서 순위 값 등 강조가 필요한 셀에 적용한다.

| 항목 | 기준 |
|------|------|
| 배경 | `color-1` ~ `color-20` 순서대로 사용 (첫 번째 강조 셀은 `color-1`, 이후 순서대로 증가. 20개 초과 시 `color-1`부터 순환) |
| 글꼴 | `Label-2-strong` |
| 텍스트 색상 | `white` |
| 하단·우측 보더 | 1px solid `neutral-300` (variant에 따라 적용) |

**평균값 강조 셀**

척도 평균값 등 수치 강조가 필요한 셀에 적용한다.

| 항목 | 기준 |
|------|------|
| 글꼴 | `Label-2-strong` |
| 텍스트 색상 | `neutral-900` |

**hover 상태**

| 항목 | 기준 |
|------|------|
| 행 hover 배경 | `neutral-50` |

**데이터 테이블 토글 버튼**

| 항목 | 기준 |
|------|------|
| 레이아웃 | flex row, items-center, gap: 4px |
| 글꼴 | `Label-1-strong` `neutral-700` |
| 아이콘 크기 | 24×24px, `neutral-700` 색상 |
| 아이콘 방향 (펼쳐진 상태) | 위 화살표 |
| 아이콘 방향 (접힌 상태) | 아래 화살표 |
| 패딩 | 6px 8px |
| 배경 | transparent |
| 보더 | 없음 |
| border-radius | 6px |
| hover 배경 | `neutral-100` |
| 표시 문구 (펼쳐진 상태) | `데이터 테이블 숨기기` |
| 표시 문구 (접힌 상태) | `데이터 테이블 펼치기` |

- 기본 상태: 펼쳐진 상태로 렌더링한다.
- 원형 그래프 보기에서도 차트 위치는 동일하며, 차트 옆에 데이터 테이블을 배치한다.

### 2-6. 툴팁

차트 요소 hover 시 fixed position으로 표시되는 정보 레이어다.

| 항목 | 기준 |
|------|------|
| 배경 | `neutral-900` |
| 텍스트 색상 | `White` |
| 패딩 | 8px 10px |
| border-radius | 4px |
| 글꼴 | 11.5px, Regular, line-height 1.6 |
| 그림자 | `0 4px 12px rgba(0,0,0,0.22)` |
| z-index | 9999 |
| pointer-events | none |
| 줄바꿈 | `white-space: nowrap` |
| 위치 | fixed (마우스 좌표 추적) |

### 2-7. 비지원 상태 영역

지원하지 않는 문항 유형이거나 집계에 필요한 데이터가 없을 때 표시한다.

| 항목 | 기준 |
|------|------|
| 배경 | `neutral-50` |
| border-radius | `--radius-8` |
| 패딩 | 24px 16px |
| 글꼴 | `Caption-1` `neutral-600` |
| 정렬 | center |
| 표시 문구 | `아직 지원하지 않는 문항 형식` |

---

## 3. 데이터 표시 규칙

### 비율과 반올림
- 비율은 내부 계산값을 유지하고, 표시 시점에만 소수점 첫째 자리까지 반올림한다.
- 반올림은 부동소수점 오차를 최소화하기 위해 `Math.round((value + 1e-10) * 10) / 10` 방식으로 처리한다.
- 응답자 수 `N`은 반올림하지 않고 정수 그대로 표시한다.
- 같은 문항 안에서는 차트, 툴팁, 표가 동일한 비율 표시 규칙을 사용한다.
- 툴팁에서는 응답 비율을 `xx.x%`, 응답 수를 `N=xxx` 형식으로 표시한다.

### 보기 정렬
- 기본 보기 순서는 코드북의 보기 정의 순서를 따른다.
- 코드북에 없는 예외 응답이 있으면 기존 보기 뒤에 추가한다.
- 전체 기준으로 응답 0건인 보기는 기본 차트와 표에서 숨긴다.
- 그룹 비교에서도 전체 기준 0건 보기는 함께 숨긴다.

### 그룹핑 규칙
- 그룹 비교 시 그룹명은 `분석 기준 문항명: 그룹 값` 형식으로 표기한다.
  - 예: `성별: 여`, `세대: Z세대`
- 그룹별로 차트를 세로로 나열하는 경우, 좌측에 표시되는 그룹명 레이블은 가로 막대 보기명과 동일한 레이아웃을 사용한다. 즉, 좌측 고정 열에 `text-right`로 배치하고 너비는 차트 너비의 20%, 최소 120px, 최대 220px으로 한다.
- 그룹 비교가 가능한 화면에서는 체크박스로 그룹별 표시 여부를 제어한다.
- 개별 그룹 on/off가 가능하다.
- 범례에 붙는 액션 버튼(`전체 선택`, `전체 해제`, `그룹 편집`, `두 그룹만 비교하기` 등)의 **유형·조합**은 **`2-4. 범례 패널` > 범례의 종류·범례 액션 버튼**을 본다.
- 해제된 그룹은 범례에서 색과 텍스트를 흐리게 표시해 비활성 상태로 구분하고, 차트와 표에서는 숨긴다.

### 기타 응답 보기
- 기타 응답용 텍스트 컬럼이 있으면 데이터 테이블의 해당 보기명 옆에 `응답 보기` 버튼을 표시할 수 있다.
- 버튼을 누르면 기타 자유응답 목록을 모달로 보여준다.
- 모달에는 중복 제거 없이 실제 수집된 텍스트 목록을 그대로 보여준다.

---

## 4. 문항 응답 형식별 시각화 규칙

### 차트 유형 옵션 요약

| 응답 형식 | 기본 차트 | 추가 옵션 | 컨트롤 방식 |
|-----------|-----------|-----------|-------------|
| 객관식 단일 | 가로 막대 | 세로 막대 / 100% 기준 누적 가로 막대 / 원형(파이) | 드롭다운 |
| 객관식 중복 | 가로 막대 | 세로 막대 | 드롭다운 |
| 객관식 순위 | 가로 롤리팝 | 세로 롤리팝 / 가로 누적 막대 / 세로 누적 막대 | 가로·세로 방향: 드롭다운 / 롤리팝·누적 막대: 토글 버튼 |
| 객관식 척도 (raw) | 가로 100% 누적 막대 | 척도 평균 그래프 / 원형(파이) | 누적 막대·원형 전환: 드롭다운 / 분포·평균 전환: 토글 버튼 (누적 막대 선택 시에만 활성) |
| 객관식 척도 (derived) | 박스플롯 | — | — |
| 주관식 숫자 | 박스플롯 | 히스토그램 | 토글 버튼 |
| 주관식 시간 | 박스플롯 | 히스토그램 | 토글 버튼 |
| 주관식 비율 배분 | 가로 100% 누적 막대 | 원형(파이) | 드롭다운 |
| 주관식 문자 | 응답 목록 박스 | — | 키워드 검색 |

### 4-1. 객관식 단일

#### 집계 기준
- 각 보기별 선택 응답 수를 집계하고, 전체 응답 수 대비 비율을 계산한다.

#### 컨트롤바
- 그래프 유형 드롭다운 + 응답 비율 내림차순 정렬 옵션(체크박스 + 텍스트). 기본은 코드북 순서다.

#### 차트
- 기본 차트는 가로 막대. 드롭다운으로 세로 막대 / 100% 기준 누적 가로 막대 / 원형(파이)으로 전환 가능.
- 객관식 단일의 가로 막대는 막대 트랙 배경색으로 `neutral-50`을 사용한다.
- `100% 기준 누적 가로 막대` 선택 시 막대·구간의 **치수·배치 골격**은 `##### 3. 가로 막대 100% 기준 누적형`을 따른다. 보기별 구간 색·하단 보기명·구간 내부 수치의 **상세**는 아래 표를 따른다.

| 항목 | 값 |
|------|----|
| 구간 색상 | 보기 순서대로 `color-1` → `color-2` → … (→ 아래 **범례** 절과 동일) |
| 구간 하단 보기명 | 각 구간 하단, 구간 너비에 맞춰 표기, 세그먼트가 지나치게 좁으면 미표시 |
| 보기명 좌우 패딩 | 8px |
| 구간 내부 수치 레이블 색상 | `white` |

#### 툴팁
- 기본: `보기명`, `비율`, `응답 수`
- 그룹 비교: `그룹명`, `보기명`, `비율`, `응답 수`

#### 범례
- 가로 막대 (기본): 범례 없음 (단색 막대)
- 세로 막대 / 100% 기준 누적 가로 막대: 보기별 색상 범례 표시 (보기 순서대로 `color-1` → `color-2` → … 순환)
- 원형(파이) 차트: 범례 섹션에 색상 swatch + 보기명만 표시한다 (체크박스 없음)

#### 그룹 비교
- 가로 막대 / 100% 기준 누적 가로 막대에서만 지원한다.
- `##### 12. 그룹별 비교` 섹션 참조.

#### 데이터 테이블
- 전체: `보기`, `응답 비율(%)`, `응답 수(명)`
- 그룹 비교: 전체 + 각 그룹의 `응답 비율(%)`, `응답 수(명)`
- 기타 응답용 텍스트 컬럼이 있으면 데이터 테이블의 `기타` 행 옆에 `응답 보기` 버튼을 표시한다. 버튼을 누르면 기타 자유응답 텍스트 목록을 모달로 보여준다.

---

### 4-2. 객관식 중복

#### 집계 기준
- 보기별 expanded 이진 컬럼을 기준으로 각 보기의 선택 비율과 응답 수를 독립 집계한다.
- 선택값으로 `1`, `y`, `yes`, `true`, `선택`을 모두 선택 처리한다.

#### 컨트롤바
- 그래프 유형 드롭다운 + 응답 비율 내림차순 정렬 옵션(체크박스 + 텍스트). 기본은 코드북 순서다.

#### 차트
- 기본 차트는 가로 막대. 드롭다운으로 세로 막대로 전환 가능.
- 객관식 중복의 가로 막대는 막대 트랙 배경색으로 `neutral-50`을 사용한다.

#### 툴팁
- 기본: `보기명`, `비율`, `응답 수`
- 그룹 비교: `그룹명`, `보기명`, `비율`, `응답 수`

#### 범례
- 기본: 없음 (단색 막대)
- 그룹 비교 시: 그룹별 범례 표시

#### 그룹 비교
- 가로 막대에서만 지원한다.
- `##### 12. 그룹별 비교` 섹션 참조.

#### 데이터 테이블
- 전체: `보기`, `응답 비율(%)`, `응답 수(명)`
- 그룹 비교: 전체 + 각 그룹의 `응답 비율(%)`, `응답 수(명)`
- 하단 안내 문구 : `객관식 중복 응답 문항으로, 보기별 비율 합계는 100%를 초과할 수 있습니다.`

---

### 4-3. 객관식 순위

#### 집계 기준
- 순위 문항은 순위별 expanded 컬럼을 기준으로 집계한다.
- 순위 expanded 컬럼명은 `__1순위` 표준 형식을 우선 사용한다. legacy 호환을 위해 `_1순위` 형식도 인식한다.
- 응답자 수 `N`은 raw 값이 있거나 순위 expanded 값이 하나라도 있으면 포함한다.
- 순위별 비율 계산 모수는 해당 문항에 참여한 전체 응답자 수를 사용한다.

#### 가중 점수 계산
- 가중치는 최하위 순위부터 1점씩 시작해 순위가 올라갈수록 2씩 증가한다.
  - 예: 3순위까지인 경우 1순위=5점, 2순위=3점, 3순위=1점
  - 예: 4순위까지인 경우 1순위=7점, 2순위=5점, 3순위=3점, 4순위=1점
- 보기별 가중 평균 점수 = 각 순위 응답 수 × 해당 순위 가중치의 합 ÷ 전체 응답자 수
- 종합 순위는 가중 평균 점수 기준 내림차순. 동률이면 공동 순위로 표시한다.

#### 비순위 응답 처리
- raw에는 값이 있지만 순위별 expanded에 실제 순위값이 없는 응답은 비순위 응답으로 본다.
- 전체 응답 수에는 포함하되, 가중 평균 점수와 종합 순위 계산에는 포함하지 않는다.

#### 기타 보기 처리
- `기타` 보기는 응답 수와 표에는 포함하되, 종합 순위 계산과 차트 표시(visibleOptionOrder)에서는 제외한다.

#### 컨트롤바
- 가중 평균 점수 내림차순 정렬 옵션(체크박스 + 텍스트). 기본은 코드북 순서다.
- 가로/세로 방향 전환: 드롭다운 (컨트롤바 좌측). 롤리팝 ↔ 누적 막대 전환: 토글 버튼 (드롭다운 우측).

#### 차트
- 기본 차트는 가로 롤리팝.
- 가로축 범위: 0점(좌끝) ~ 가중 평균 점수의 **만점(최고 점수)**에 맞춘 우끝. 만점은 순위 수에 따라 결정된다. 예: 3순위까지면 5점, 4순위까지면 7점.
- 각 보기의 우측 레이블에는 `가중 평균 점수(종합 순위)` 표기. 예: `3.2(1위)`
- 누적 막대 차트에서는 각 보기 안에서 `1순위`, `2순위`, `3순위` 등을 색상 구간으로 나눠 표시한다.
- 차트 하단 안내 문구: 가중 평균 산출 방식을 문장으로 풀어쓰고 산출식과 만점을 병기한다. 예: `각 보기의 가중 평균 점수는 순위별 응답 수에 가중치를 곱해 합산한 뒤 전체 응답자 수로 나눈 값입니다. 예) 3순위까지 선택하는 경우: (1순위×5 + 2순위×3 + 3순위×1) ÷ 전체 응답자 수 (만점: 5점)`

#### 툴팁
- 롤리팝: `보기명`, `가중 평균 점수`, `종합 순위`
- 누적 막대 순위 구간: `보기명`, `해당 순위`, `비율`, `응답 수`

#### 범례
- 기본: 누적 막대 보기에서만 순위별 범례를 표시한다.

#### 그룹 비교
- 가로 롤리팝에서만 지원한다.
- `##### 12. 그룹별 비교` 섹션 참조.

#### 데이터 테이블
- 보기별 순위별 `응답 비율(%)`, `응답 수(명)` + 우측에 `가중 평균`, `종합 순위` 컬럼
- 그룹 비교: 전체 순위 정보 + 그룹별 순위 정보
- 합계 행의 `가중 평균`, `종합 순위`는 `-`로 표시한다.

---

### 4-4. 객관식 척도

척도 문항은 `raw 척도`와 `derived 척도`를 구분해 처리한다.

#### raw 척도 집계 기준
- 각 점수별 응답 수를 집계하고, 전체 응답 수 대비 비율과 평균값을 계산한다.

#### raw 척도 컨트롤바
- 차트 유형 드롭다운: `100% 기준 누적 막대` / `원형(파이)` 전환.
- 분포 보기 ↔ 평균 보기 전환: 토글 버튼. `100% 기준 누적 막대` 선택 시에만 활성화하며, 원형 선택 시 비활성화한다.
- 홀수 척도는 `중간값 제외 보기` 체크박스를 추가 제공한다. 원형 보기에서는 비활성화한다.
- `중간값 제외 보기`는 `5점 척도`에서는 `3점`, `7점 척도`에서는 `4점` 막대를 차트에서만 숨긴다.
- 이 기능은 시각적 표현에만 적용한다. 숨겨진 막대를 제외한 나머지 막대가 100%를 다시 채워 보이지만, 각 점수의 응답 비율 숫자 자체는 원본 값을 유지하며 바뀌지 않는다.
- 안내 문구 `중간값 제외 보기는 차트에서만 시각적으로 제외하며, 응답 비율의 계산 모수와 원본 수치는 바뀌지 않습니다.`를 컨트롤바 하단에 표시한다.

#### raw 척도 차트
- 분포 보기: 점수별 응답 분포를 가로 100% 누적 막대로 표시. 하위/상위 척도 응답 합계 요약 함께 표시.
- 평균 보기: 척도 개수만큼 눈금이 있는 축 위에 평균 마커 표시. (`100% 기준 누적 막대` 선택 시에만 전환 가능)
- 원형(파이) 보기: 점수별 응답 비율을 원형 차트로 표시. 색상은 척도 계열 컬러 적용. 범례에 체크박스 없이 색상 swatch + 점수 + 레이블 표시.
- 분포 보기(가로 100% 누적 막대·원형)의 구간 색상은 1-2 **척도 계열**을 따른다.

#### raw 척도 — 분포 보기(가로 100% 누적 막대) 레이블·색 상세

막대 높이·구간 너비·구간 내부 수치 중앙 배치·하단 레이블 ↔ 막대 간격·하단 이중 행(텍스트 레이블 `Label-1`, 수치 레이블 `Label-1-strong`, 줄 간격 0) 등 **공통 치수**는 `##### 3. 가로 막대 100% 기준 누적형`을 따른다. 아래는 척도 분포 보기에만 해당한다.

객관식 척도 분포 보기에서는 보기명 대신 하위/상위 척도 합계를 막대 양끝 하단에 표시한다.

| 항목 | 값 |
|------|----|
| 구간 색상 | 1-2 **척도 계열** |
| 구간 내부 수치 레이블 색상 | `white` |
| 하위 척도 합계 레이블 | 막대 좌끝 하단, 좌측 정렬 |
| 상위 척도 합계 레이블 | 막대 우끝 하단, 우측 정렬 |
| 레이블 구조 | 텍스트("하위" / "상위")와 수치(응답 비율)를 세로로 나열. 텍스트 ↔ 수치 간격 0 |
| 레이블 ↔ 막대 gap | 8px (`##### 3`과 동일) |
| 텍스트 스타일 | `Label-1` `neutral-700` |
| 수치 스타일 | `Label-1-strong` `neutral-700` |

#### raw 척도 툴팁
- 분포 보기: `점수값 또는 점수 레이블`, `비율`, `응답 수` (그룹 비교 시 `그룹명` 추가)
- 평균 보기: `평균값`, `응답 수` (그룹 비교 시 `그룹명` 추가)

#### raw 척도 범례
- 점수별 색상 / 점수 / 레이블을 표시한다. 예: `붉은색 swatch + 1점 + 전혀 그렇지 않다`

#### raw 척도 그룹 비교
- 그룹 비교 시에는 같은 형식의 차트를 그룹별로 세로로 나열해 비교한다.
- 그룹별 차트 간 세로 간격은 16px로 유지한다.
- 그룹 범례 및 비교 컨트롤은 범례 영역이 아니라 차트 영역 상단에 배치한다.
- 그룹 비교 컨트롤과 차트 사이 간격은 40px로 유지한다.

#### raw 척도 데이터 테이블
- 단일 전체: `점수`, `응답 비율(%)`, `응답 수(명)`, `평균` (평균 값은 합계 행에만 표시)
- 단일 그룹 비교: 전체 + 각 그룹의 `점수`, `응답 비율(%)`, `응답 수(명)`, `평균` (평균은 각 블록 합계 행에만 표시)

#### derived 척도 집계 기준
- 계산된 연속형 점수를 기준으로 최소값, 최대값, 평균, 사분위수(`Q1`, `Q2`, `Q3`)를 계산한다.

#### derived 척도 컨트롤바
- 분포 보기 ↔ 평균 보기 전환: 토글 버튼.

#### derived 척도 차트
- `type = scale` 이면서 `role = derived`인 경우 derived 척도로 본다.
- 분포 보기: 박스플롯. `Q1`, `Q2`, `Q3` 마커 하단에 레이블과 값을 세로로 표시한다. (→ 2-3 박스플롯 스펙 참조)
- 평균 보기: 척도 평균 그래프. (→ 2-3 척도 평균 그래프 스펙 참조)

#### derived 척도 툴팁
- 사분위수 마커: `사분위명`, `사분위값` (툴팁에서는 `Q1(하위 25%)`, `Q2(중앙값)`, `Q3(상위 25%)` 형식)
- 평균 보기: `평균값`, `응답 수` (그룹 비교 시 `그룹명` 추가)

#### derived 척도 범례
- 없음

#### derived 척도 그룹 비교
- 그룹 비교 시에는 같은 형식의 차트를 그룹별로 세로로 나열해 비교한다.
- 그룹별 차트 간 세로 간격은 16px로 유지한다.
- 그룹 범례 및 비교 컨트롤은 범례 영역이 아니라 차트 영역 상단에 배치한다.
- 그룹 비교 컨트롤과 차트 사이 간격은 40px로 유지한다.

#### derived 척도 데이터 테이블
- 단일: `문항명`, `최소값`, `Q1`, `Q2(중앙값)`, `Q3`, `최대값`, `평균`, `응답 수(명)`
- 단일 그룹 비교: `그룹명`, `최소값`, `Q1`, `Q2(중앙값)`, `Q3`, `최대값`, `평균`, `응답 수(명)` 구조로 표시하고, 표 상단에 `전체` 행을 별도로 둔다.

#### 척도형 여러 문항 비교
- 같은 척도 길이의 문항끼리 `여러 문항 한 번에 비교하기` 사용 가능.
- 여러 문항 비교 대상에는 `raw 척도`와 `derived 척도`가 함께 포함될 수 있다.
- 비교 대상 문항이 모두 같은 척도형이면 버튼 자동 활성화. 하나라도 다르면 비활성화.
- 그룹이 없으면 `분포 보기`와 `평균 보기`를 모두 사용할 수 있다.
- 그룹이 있으면 `분포 보기`는 사용할 수 없고 `평균 보기`로 고정한다.
- 여러 문항 비교 대상에 `raw 척도`가 하나라도 포함되어 있으면 `중간값 제외 보기` 체크박스를 활성화한다.
- 여러 문항 비교 (그룹 없음) 데이터 테이블:
- `raw 척도` 문항은 각 문항별 `문항명`, 각 점수별 `응답 비율(%)`, `응답 수(명)`, `평균`, `전체 응답 수(명)`을 표시한다.
- `derived 척도` 문항은 각 문항별 `문항명`, `평균`, `응답 수(명)`을 표시한다.
- 여러 문항 비교 (그룹 있음) 데이터 테이블: `raw 척도`와 `derived 척도` 모두 각 문항별 `문항명`, `전체 평균`, `전체 응답 수(명)`, 각 그룹의 `평균`, `응답 수(명)`을 표시한다.

---

### 4-5. 주관식 숫자

#### 집계 기준
- 숫자 응답값을 기준으로 최소값, 최대값, 평균, 사분위수(`Q1`, `Q2`, `Q3`)를 계산한다.
- 히스토그램 보기에서는 구간별 응답 수와 비율을 계산한다.

#### 컨트롤바
- 기본은 박스플롯이다.
- 박스플롯 ↔ 히스토그램 전환: 토글 버튼 (`요약 보기` / `분포 보기`). 그룹 비교 시 박스플롯으로 고정.
- 안내 문구 `구간 시작값과 간격을 조정해 분포를 원하는 기준으로 확인할 수 있습니다.`는 `분포 보기` 선택 상태일 때만 컨트롤바 하단에 표시한다.

#### 차트 — 박스플롯
- 최소값~최대값 whisker, `Q1~Q3` box, 중앙값 기준선, 평균 마커를 표시한다.
- 평균 마커는 중앙값 기준선과 겹칠 수 있으므로, 중앙값 기준선보다 위 레이어에 렌더링한다.
- 각 요소 의미를 설명하는 안내 문구를 함께 제공한다.

#### 차트 — 히스토그램
- 사용자가 구간 시작값과 구간 간격을 조정할 수 있다.
- 평균 마커를 표시한다.

#### 툴팁
- 히스토그램: `구간 범위`, `응답 수`
- 평균 마커: `평균값`, `응답 수`
- 사분위수 마커: `사분위명`, `사분위값` (툴팁에서는 `Q1(하위 25%)`, `Q2(중앙값)`, `Q3(상위 25%)` 형식)
- 박스플롯: `최소값`, `Q1(하위 25%)`, `Q2(중앙값)`, `Q3(상위 25%)`, `최대값`, `평균값`

#### 범례
- 없음

#### 그룹 비교
- 그룹 비교 시에는 박스플롯을 그룹별로 세로로 나열해 비교한다.
- 그룹별 차트 간 세로 간격은 16px로 유지한다.

#### 데이터 테이블
- 전체: `문항명`, `최소값`, `Q1`, `Q2(중앙값)`, `Q3`, `최대값`, `평균`, `응답 수(명)`
- 그룹 비교: `그룹명` 기준, 표 상단에 전체 행 별도 표시

---

### 4-6. 주관식 비율 배분

#### 집계 기준
- raw 컬럼에는 배분 항목 순서대로 `40|60`처럼 파이프 구분 숫자값이 들어갈 수 있다.
- expanded 컬럼이 있으면 `{원문항 라벨}__{배분 항목명}` 컬럼의 항목별 숫자값을 우선 사용한다.
- expanded 컬럼이 없으면 raw 값의 파이프 순서를 코드북의 `response_options` 순서에 맞춰 해석한다.
- 각 응답자의 항목별 합계가 100인 응답만 유효 응답으로 집계한다.
- 항목별 평균 배분값은 유효 응답자별 항목 값을 평균해 계산한다.

#### 차트
- 가로 100% 누적 막대. 각 구간 너비는 항목별 평균 배분값을 나타낸다.
- 막대·구간의 **치수·배치 골격**은 `##### 3. 가로 막대 100% 기준 누적형`을 따른다.
- 각 막대 하단에 항목별 레이블과 평균 배분값을 표시한다. 하단 레이블이 두 줄로 쌓이는 경우 **타이포그래피·정렬 계열**은 4-4 **raw 척도 — 분포 보기(가로 100% 누적 막대) 레이블·색 상세**에 정한 이중 행 규칙(텍스트 `Label-1`, 수치 `Label-1-strong`, 줄 간격 0 등)과 동일하게 적용한다.

#### 툴팁
- `그룹명`, `배분 항목`, `평균 배분값(%)`, `응답 수`

#### 범례
- 항목 구간은 배분 항목별 색상으로 구분한다. 색상은 `color-1`부터 항목 순서대로 적용한다.

#### 그룹 비교
- 그룹 비교 시에는 그룹별 가로 누적 막대를 세로로 나열한다.

#### 데이터 테이블
- 전체: `배분 항목`, `평균 배분값(%)`, `응답 수(명)`
- 그룹 비교: 전체 + 각 그룹의 `평균 배분값(%)`, `응답 수(명)`

---

### 4-7. 주관식 시간

#### 집계 기준
- 코드북에서 해당 문항의 `{question_label}_분환산` derived 행의 컬럼 값(분 단위 정수)을 집계에 사용한다.
- 집계 방식(최소값·최대값·평균·사분위수)은 주관식 숫자와 동일하다.

#### 차트
- 주관식 숫자와 동일하게 박스플롯(기본) / 히스토그램(분포 보기)을 사용한다.
- 모든 수치 레이블은 분 단위 값을 `hh:mm` 형식으로 변환해 표시한다. (예: 540분 → `09:00`)

#### 툴팁
- 주관식 숫자와 동일. 수치는 `hh:mm` 형식으로 표시한다.

#### 범례
- 없음

#### 그룹 비교
- 주관식 숫자와 동일하게 박스플롯을 그룹별로 세로로 나열한다.

#### 데이터 테이블
- 주관식 숫자와 동일한 구조. 수치는 `hh:mm` 형식으로 표시한다.
- 전체: `문항명`, `최소값`, `Q1`, `Q2(중앙값)`, `Q3`, `최대값`, `평균`, `응답 수(명)`
- 그룹 비교: `그룹명` 기준, 표 상단에 전체 행 별도 표시

---

### 4-8. 주관식 문자

#### 집계 기준
- 비어 있지 않은 텍스트 응답을 유효 응답으로 본다.
- 응답 텍스트는 중복 제거 없이 실제 수집된 값을 그대로 표시한다.

#### 컨트롤바
- 키워드 검색 input을 제공한다.
- 사용자가 키워드를 입력하면 해당 키워드를 포함하는 응답만 즉시 필터링한다.
- 검색어가 비어 있으면 전체 유효 응답을 표시한다.

#### 응답 목록
- 차트 대신 스크롤 가능한 응답 목록 박스를 표시한다.
- 박스 상단에는 `응답 {검색 결과 수}건 / 전체 {전체 유효 응답 수}명` 형식의 요약을 표시한다.
- 박스 본문에는 각 응답을 한 줄 또는 문단 단위로 세로 나열한다.
- 응답이 많으면 박스 내부에서만 세로 스크롤한다.

#### 툴팁
- 없음

#### 범례
- 없음

#### 그룹 비교
- 지원하지 않는다.

#### 데이터 테이블
- 별도 데이터 테이블은 표시하지 않는다.

---

## 5. 상호작용 규칙

### 그룹 on/off
- 그룹 비교 차트에서 표시할 그룹을 사용자가 직접 선택할 수 있다.
- 선택 해제된 그룹은 범례에서는 흐리게 표시되고, 차트와 표에서는 숨겨진다.
- 체크 상태 변경은 현재 표시 중인 차트와 데이터 테이블에 즉시 반영한다.

### 그래프 유형 전환
- 드롭다운을 사용하는 차트 유형에서는, 문항 형식별로 허용된 그래프 유형 옵션을 선택하면 즉시 재렌더링한다.
- 정렬 변경은 현재 선택된 그래프 유형에 동일하게 적용한다.

### 사용자 정의 그룹 편집
- 여기서의 `그룹 편집`은 `그룹 비교`의 그룹과 다른 개념이다. 버튼 이름은 `그룹 편집`이지만, 기능 의미는 기존 응답 보기(항목) 여러 개를 사용자가 원하는 `사용자 정의 묶음`으로 다시 묶는 것이다.
- 이 문서에서는 이 기능을 설명할 때 `사용자 정의 묶음`이라는 용어를 사용한다.
- 예: `직급` 문항의 보기인 `팀장`, `임원`을 하나의 사용자 정의 묶음 `리더`로 합쳐 볼 수 있다.
- 그룹 범례 액션 영역의 `그룹 편집` 버튼을 클릭하면 모달을 열어 사용자 정의 묶음의 생성/삭제/이름 변경/항목 매핑을 수행한다.
- 모달을 닫으면 변경 사항을 즉시 반영한다.
- 묶음 적용 상태에서는 범례에서 각 응답 보기 항목이 어느 사용자 정의 묶음에 속하는지(색 인덱스 포함) 확인할 수 있다.
- 묶음 해제는 `원래대로 보기` 버튼으로 즉시 복귀한다.

---

## 6. 기타 응답 처리 규칙

### 기타 텍스트 컬럼 탐색
- 기타 자유응답 텍스트는 전용 expanded 컬럼에서 읽는다.
- 현재 구현은 아래와 같은 이름 패턴을 순차적으로 허용한다.
  - `__기타_텍스트`
  - `__기타 텍스트`
  - `_기타_텍스트`
  - `_텍스트`
- 위 패턴에 정확히 맞지 않아도, 문항명과 기타 텍스트 의미가 함께 드러나는 경우 느슨한 fallback 탐색을 수행한다.

### 기타 응답 표시
- 기타 자유응답이 존재하면 데이터 테이블에서 `응답 보기` 버튼을 노출할 수 있다.
- 모달에서는 중복 제거 없이 실제 응답 텍스트 목록을 보여준다.
- 기타 응답은 차트 표시보다 상세 확인 보조 기능에 가깝게 다룬다.

---

## 7. 예외 및 비지원 처리

### 데이터 부족
- 문항은 지원 유형이더라도 실제 집계에 필요한 컬럼이 없으면 결과를 그리지 못할 수 있다.
- 이 경우 비지원 또는 표시 불가 상태의 안내 영역을 보여준다. (컴포넌트 스펙은 **2-7** 참조)

### 순위 문항 컬럼 불완전
- 순위형은 순위별 expanded 컬럼을 찾지 못하면 정상 집계를 수행하지 않는다.
- raw 값만 있다고 해서 순위 차트를 강제로 생성하지 않는다.

### 중복 문항 컬럼 불완전
- 중복형은 보기별 expanded 이진 컬럼을 찾지 못하면 정상 집계를 수행하지 않는다.

---

## 8. 문서 운영 원칙

- 문서보다 실제 구현이 우선이다.
- `dashboard.html`, `visualizations.js` 동작이 바뀌면 이 문서도 함께 갱신한다.
- 문서에는 기획안이 아니라 현재 화면에서 실제로 보이는 규칙을 적는다.
- 구현에서 `design_system.md` 토큰 목록에 없는 임의 색상·크기 값을 사용하게 되면 이 문서의 **1. 디자인 토큰** 섹션을 먼저 갱신하고 사용자에게 확인을 받는다.

---
